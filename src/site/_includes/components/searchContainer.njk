<div class="search-container" id="globalsearch" role="dialog" aria-modal="true" aria-hidden="true" aria-labelledby="search-dialog-title" onclick="closeSearch()">
    <div class="search-box" onclick="event.stopPropagation()">
        <h2 class="sr-only" id="search-dialog-title">Search notes</h2>
        <button type="button" class="search-close" onclick="closeSearch()" aria-label="Close search">&times;</button>
        <div class="search-header">
            <label class="sr-only" for="term">Search SFRad notes</label>
            <input type="search" id="term" placeholder="{{ meta.uiStrings.searchPlaceholder }}" autocomplete="off" spellcheck="false" role="combobox" aria-autocomplete="list" aria-controls="search-results" aria-expanded="false">
        </div>

        <div class="search-layout no-preview" id="search-layout">
            <!-- Left panel: Results list -->
            <div class="search-results-panel" id="search-results" role="listbox" aria-label="Search results">
                <!-- Default text -->
                <p class="search-status" role="status"><i>{{ meta.uiStrings.searchNotStarted }}</i></p>
            </div>

            <!-- Right panel: Content preview (hidden until search starts) -->
            <div class="search-preview-panel" id="search-preview">
                <div class="preview-placeholder">
                    <i data-lucide="file-text"></i>
                    <p>{{ meta.uiStrings.searchPreviewPlaceholder | default("Select a result to preview") }}</p>
                </div>
                <div class="preview-content" id="preview-content"></div>
            </div>
        </div>

        <footer class="search-box-footer">
            <div class="navigation-hint">
                <span><code>{{ meta.uiStrings.searchEnterHotkey }}</code>&nbsp;{{ meta.uiStrings.searchEnterHint }}</span>
            </div>

            <div class="navigation-hint">
                <span><code>{{ meta.uiStrings.searchNavigateHotkey }}</code>&nbsp;{{ meta.uiStrings.searchNavigateHint }}</span>
            </div>

            <div class="navigation-hint">
                <span><code>{{ meta.uiStrings.searchCloseHotkey }}</code>&nbsp;{{ meta.uiStrings.searchCloseHint }}</span>
            </div>
        </footer>
    </div>
</div>
{%include "components/searchScript.njk"%}
