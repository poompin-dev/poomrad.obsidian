<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* =========================================
   THE ARCHITECTURAL FIX
   ========================================= */

/* 1. THE BODY (Vertical Stack) */
/* We tell the body to stack items: Header, Wrapper, Footer */
body {
    display: flex !important;
    flex-direction: column !important;
    min-height: 100vh !important;
    margin: 0 !important;
}

/* 2. THE NEW WRAPPER (Horizontal Row) */
/* This restores your side-by-side layout (Content | Sidebar) */
/* It also 'grows' to push the footer down */
.page-wrapper {
    display: flex !important;
    flex-direction: row !important; /* Keep sidebars on the side! */
    flex: 1 0 auto !important;      /* Fill all available vertical space */
    width: 100%;
    justify-content: center;        /* Keep content centered */
}

/* 3. MAIN CONTENT ADJUSTMENT */
/* Ensure it plays nice inside the wrapper */
main.content {
    margin-top: 0 !important;
    padding-top: 40px !important;
    width: 100% !important;
    max-width: var(--layout-max) !important;
}

/* 4. THE FOOTER (Natural Flow) */
.custom-footer {
    /* No hacks needed. It just sits at the bottom now. */
    width: 100% !important;
    flex-shrink: 0 !important;      /* Prevent squishing */
    
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    margin-top: 4rem !important;
    padding: 30px 0 !important;
    z-index: 10;
}

/* 5. FOOTER CONTENT */
.footer-content {
    width: 100%;
    max-width: var(--layout-max);
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* 6. MOBILE RESPONSIVENESS */
/* On small screens, stack the wrapper content vertically */
@media screen and (max-width: 1000px) {
    .page-wrapper {
        flex-direction: column !important;
        align-items: center;
    }
}

/* Typography */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
