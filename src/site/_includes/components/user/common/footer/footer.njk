<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* =========================================
   THE "ABSOLUTE" FIX (Layout Safe)
   ========================================= */

/* 1. PREPARE THE PAGE (The Container) */
body {
    position: relative !important;   /* Creates the coordinate system for the footer */
    min-height: 100vh !important;    /* Ensures page is at least as tall as screen */
    padding-bottom: 12rem !important; /* CRITICAL: Reserves space at bottom so footer doesn't cover text */
    overflow-x: hidden;              /* Prevents scrollbar from 100% width */
}

/* 2. THE FOOTER (Taken OUT of the flow) */
.custom-footer {
    position: absolute !important;   /* Removes it from the flex/grid layout entirely */
    bottom: 0 !important;            /* Pins it to the very bottom */
    left: 0 !important;              /* Pins it to the left edge */
    
    width: 100% !important;          /* Forces full width */
    height: 12rem !important;        /* Fixed height (matches body padding above) */
    
    /* Alignment & Visuals */
    display: flex;
    align-items: center;             /* Vertically center the content */
    justify-content: center;
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    z-index: 100;
}

/* 3. CENTER THE MAIN TEXT (The specific fix you asked for) */
/* This ensures the middle column stays in the middle */
main.content, .markdown-preview-view {
    margin-left: auto !important;
    margin-right: auto !important;
    
    /* Ensure it doesn't get too wide or too narrow */
    width: 100% !important;
    max-width: var(--layout-max, 900px) !important; 
}

/* 4. FOOTER CONTENT */
.footer-content {
    width: 100%;
    max-width: var(--layout-max, 900px);
    text-align: center;
    padding: 0 20px;
}

/* Typography */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
    line-height: 1.6;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 4px;
}
</style>
