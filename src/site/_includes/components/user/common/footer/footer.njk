<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. EMERGENCY RESET: Stop the body from flexing */
/* This restores the sidebar/main layout to normal */
body {
    display: block !important;
    position: relative;
}

/* 2. FORCE TEXT CENTERING */
/* This grabs the main text column and locks it to the middle */
main.content, .markdown-preview-view {
    margin-left: auto !important;
    margin-right: auto !important;
    float: none !important; /* Prevents it from drifting left */
    
    /* Ensure it respects your width settings */
    max-width: var(--layout-max) !important;
    width: 100% !important;
    
    /* Restore top spacing so it doesn't hide behind navbar */
    padding-top: 40px !important; 
}

/* 3. FOOTER (Simplified) */
/* We stop the '100vw' hack for now to ensure stability. 
   It will just sit safely at the bottom. */
.custom-footer {
    width: 100%;
    margin-top: 5rem !important; /* Push it down away from text */
    padding: 30px 0;
    
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    text-align: center;
}

.footer-content {
    margin: 0 auto;
    max-width: var(--layout-max);
}

.footer-content p {
    color: var(--text-muted);
    font-size: 0.9em;
    margin: 0;
}
</style>
