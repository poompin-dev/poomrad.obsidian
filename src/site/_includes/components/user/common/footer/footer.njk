<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  const yearSpan = document.getElementById("year-display");
  if(yearSpan) {
    yearSpan.innerText = new Date().getFullYear();
  }
</script>

<style>
/* =========================================
   MASTER CSS: TRANSPARENT & EXPANDED
   ========================================= */

/* 1. SCROLL ENGINE */
html {
    overflow-x: hidden !important;
    background-color: var(--background-primary) !important;
}

body {
    display: flex !important;
    flex-direction: column !important;
    min-height: 100vh !important;
    margin: 0 !important;
    padding: 0 !important;
    overflow-y: auto !important;    
    overflow-x: hidden !important; 
    background-color: var(--background-primary) !important;
    color: var(--text-normal) !important;
}

/* 2. PAGE WRAPPER */
.page-wrapper {
    flex: 1 0 auto !important; 
    display: flex !important;
    flex-direction: column !important; 
    align-items: center !important;    
    width: 100%;
    margin: 0 auto; 
    padding-top: 80px !important;       
}

/* Hide Separator if no backlinks */
.page-wrapper:not(:has(.backlink-card)) .stacked-separator {
    display: none !important;
}

/* 3. MAIN CONTENT */
main.content {
    width: 100% !important;
    max-width: var(--dg-content-max-width, 900px) !important;
    margin: 0 auto !important;
    padding: 0 20px !important;
    overflow: visible !important; 
}

/* =========================================
   4. SEPARATOR LINE
   ========================================= */
.stacked-separator {
    display: block;
    border: 0;
    height: 1px;
    background: var(--text-normal);
    opacity: 0.3; 
    
    /* Match content width */
    width: 90% !important;
    max-width: 900px !important;
    
    margin-left: auto !important;
    margin-right: auto !important;
    margin-top: 60px !important;
    margin-bottom: 40px !important;
}

/* =========================================
   5. SIDEBAR CONTAINER (Flex Parent)
   ========================================= */
aside, .sidebar, .right-sidebar, .sidebar-container {
    position: static !important;
    display: flex !important;       
    flex-direction: column !important;
    align-items: center !important; /* Centers the list */
    width: 100% !important;         
    margin: 0 !important;
    padding: 0 !important;
    
    /* Reset Floating */
    float: none !important;
    top: auto !important;
    left: auto !important;
    transform: none !important;
}

/* =========================================
   6. CLEAN BACKLINKS (No Box, Expanded)
   ========================================= */
.backlinks, .toc, .backlink-list {
    /* 1. REMOVE BOX STYLING */
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;

    /* 2. FIT TO TEXT */
    width: fit-content !important;  
    min-width: 200px !important;    
    max-width: 95vw !important;     
    
    /* 3. EXPAND HEIGHT (No Scroll) */
    height: auto !important;
    max-height: none !important;
    overflow: visible !important;

    /* 4. ALIGNMENT */
    margin: 0 auto !important;      
    margin-bottom: 40px !important;
    padding: 0 !important;          
}

/* The List Items */
.backlink-list, .backlinks-list-container {
    height: auto !important;
    overflow: visible !important;
}

/* The Link Text */
.backlink a, .internal-link, .backlink-header {
    white-space: nowrap !important; /* No wrapping */
    text-overflow: ellipsis !important;
}

/* Header Styling (optional cleanup) */
.backlinks h3, .backlink-header {
    margin-top: 0 !important;
    opacity: 0.7 !important;
    text-align: center !important; 
    font-size: 0.9em !important;
    text-transform: uppercase !important;
    letter-spacing: 1px !important;
}

.graph-view-container { display: none !important; }

/* =========================================
   8. DESKTOP FLOAT (> 1650px ONLY)
   ========================================= */
@media screen and (min-width: 1650px) {
    aside, .sidebar, .right-sidebar {
        display: block !important;
        position: fixed !important;
        top: 100px !important;
        
        /* Positioning */
        left: calc(50% + 470px) !important;
        
        /* Float Logic: Fit content but cap it so it doesn't fly off screen */
        width: fit-content !important;
        max-width: 400px !important;
        
        bottom: auto !important;
        z-index: 100 !important;
    }
    
    /* Hide separator on desktop */
    .stacked-separator { display: none !important; }
    
    /* Ensure the float is also transparent/clean */
    .backlinks, .toc {
        width: 100% !important;
        background: transparent !important;
        border: none !important;
        box-shadow: none !important;
        text-align: left !important; /* Align left for side reading */
    }
    
    /* Left align title on desktop for neatness */
    .backlinks h3, .backlink-header {
        text-align: left !important;
    }
}

/* =========================================
   9. FOOTER STYLING
   ========================================= */
.custom-footer {
    flex-shrink: 0 !important;
    width: 100% !important;
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    padding: 40px 0 !important;
    margin-top: auto !important;
    z-index: 10;
}

.footer-content {
    width: 100%;
    max-width: var(--dg-content-max-width, 900px);
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
