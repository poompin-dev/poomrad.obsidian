<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. SETUP THE PAGE (The Stage) */
body {
    position: relative;
    min-height: 100vh;
    /* REMOVED: padding-bottom is no longer needed because the footer takes up real space now */
    padding-bottom: 0 !important; 
    box-sizing: border-box;
    
    /* Ensure the body flexes so footer can be pushed down */
    display: flex;
    flex-direction: column;
}

/* 2. THE FOOTER (Natural Flow) */
.custom-footer {
    /* CHANGED: From absolute to relative. 
       Now it sits naturally AFTER the content, not on top of it. */
    position: relative; 
    width: 100%;
    height: auto;
    
    /* Push it to the bottom if the page is short */
    margin-top: auto; 
    
    /* Visuals */
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    padding: 30px 0;
    z-index: 10; /* Lower z-index so it doesn't fight the navbar */
}

/* 3. THE INNER CONTENT */
.footer-content {
    width: 100%;
    max-width: var(--layout-max);
    margin: 0 auto;
    padding: 0 20px;
    
    /* FORCE CENTERING */
    display: flex;
    flex-direction: column;
    align-items: center; 
    justify-content: center;
    text-align: center;
}

/* 4. TEXT STYLING */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-ui);
    font-size: 0.9em;
    line-height: 1.5;
    text-align: center !important;
    width: 100%;
}

.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}

.custom-footer a {
    color: var(--text-muted);
    text-decoration: underline;
    text-decoration-style: dotted;
}
.custom-footer a:hover {
    color: var(--interactive-accent);
}
</style>
