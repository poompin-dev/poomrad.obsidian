<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. RESET THE PAGE LAYOUT (The Holy Grail Fix) */
body {
    display: flex !important;
    flex-direction: column !important;
    min-height: 100vh !important;
    margin: 0 !important;
    padding: 0 !important;
    width: 100% !important;
    overflow-x: hidden !important; /* Safety for mobile */
}

/* 2. MAKE MAIN CONTENT GROW */
/* This forces the content to take up all available space, 
   pushing the footer to the bottom naturally. */
main.content {
    flex: 1 0 auto !important;
    width: 100% !important;
    max-width: var(--layout-max) !important;
    margin-left: auto !important;
    margin-right: auto !important;
    padding-top: 60px !important; /* Space for Navbar */
}

/* 3. THE FOOTER (Simplified) */
.custom-footer {
    /* No more crazy calculations */
    width: 100% !important;
    flex-shrink: 0 !important; /* Don't let it squish */
    
    /* Visuals */
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    margin-top: 4rem !important;
    padding: 40px 0 !important;
    z-index: 10;
}

/* 4. INNER CONTENT (Centered) */
.footer-content {
    width: 100%;
    max-width: var(--layout-max);
    margin: 0 auto; /* Center in the footer strip */
    padding: 0 20px;
    
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Text Styles */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-ui);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
