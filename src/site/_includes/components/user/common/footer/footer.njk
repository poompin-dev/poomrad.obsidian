<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  const yearSpan = document.getElementById("year-display");
  if(yearSpan) {
    yearSpan.innerText = new Date().getFullYear();
  }
</script>

<style>
/* =========================================
   FINAL LAYOUT: iPad Landscape Fix
   ========================================= */

/* 1. BODY */
body {
    display: grid !important;
    grid-template-rows: 1fr auto !important; 
    min-height: 100vh !important;
    margin: 0 !important;
    padding: 0 !important;
    overflow-y: auto !important;   
    overflow-x: hidden !important; 
}

/* 2. PAGE WRAPPER */
.page-wrapper {
    width: 100%;
    max-width: var(--layout-max, 1400px); 
    margin: 0 auto; 
    
    /* Gap for Navbar */
    padding-top: 100px !important; 
    
    display: flex !important;
    flex-direction: row !important;
    justify-content: center; 
    align-items: flex-start;
    
    gap: var(--layout-gap, 40px);
}

/* 3. MAIN CONTENT (Desktop Defaults) */
main.content {
    margin-top: 0 !important;
    padding-top: 0 !important; 
    
    /* DESKTOP: Lock to left/center via Flexbox */
    margin-left: 0 !important;
    margin-right: 0 !important;
    
    flex: 1 1 auto !important;
    width: 100% !important;
    max-width: var(--dg-content-max-width, 900px) !important;
}

/* 4. THE FOOTER */
.custom-footer {
    grid-row: 2 !important; 
    width: 100% !important;
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    padding: 40px 0 !important;
    margin-top: 4rem !important;
    z-index: 10;
}

.footer-content {
    width: 100%;
    max-width: var(--dg-content-max-width, 900px);
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* 5. MOBILE & TABLET RESPONSIVENESS (The Fix) */
/* Covers iPad Pro, iPad Mini Landscape, etc. */
@media screen and (max-width: 1199px) {
    .page-wrapper {
        flex-direction: column !important;
        align-items: center !important;
        gap: 20px;
        padding-top: 80px !important; 
    }
    
    main.content {
        padding-left: 20px !important;
        padding-right: 20px !important;
        
        /* IPAD FIX: Reset margins to AUTO so it floats in the center */
        margin-left: auto !important;
        margin-right: auto !important;
        
        /* Ensure it doesn't get stuck at a fixed width */
        width: 100% !important;
        max-width: 900px !important; 
    }
}

/* Typography */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
