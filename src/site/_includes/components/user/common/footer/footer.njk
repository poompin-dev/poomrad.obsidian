<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. THE FOOTER (Breakout Logic) */
.custom-footer {
    /* FORCE FULL WIDTH (The Magic) */
    width: 100vw !important; 
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;

    /* Spacing */
    margin-top: 4rem !important; /* Push away from content */
    padding: 30px 0;
    
    /* Visuals */
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    z-index: 10;
}

/* 2. THE INNER CONTENT (Re-align Text) */
.footer-content {
    /* Bring text back to the center column */
    width: 100%;
    max-width: var(--layout-max); /* Matches your note width */
    margin: 0 auto;
    padding: 0 20px;
    
    /* Flex alignment */
    display: flex;
    flex-direction: column;
    align-items: center; 
    justify-content: center;
    text-align: center;
}

/* 3. TEXT STYLING */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-ui);
    font-size: 0.9em;
    line-height: 1.5;
}

.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}

.custom-footer a {
    color: var(--text-muted);
    text-decoration: underline;
    text-decoration-style: dotted;
}
.custom-footer a:hover {
    color: var(--interactive-accent);
}

/* 4. SCROLLBAR SAFETY */
/* Prevents horizontal scrollbar on Windows due to 100vw */
body {
    overflow-x: hidden;
}
</style>
