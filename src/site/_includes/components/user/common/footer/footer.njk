<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* =========================================
   THE "GRID" FIX (Robust Layout)
   ========================================= */

/* 1. BODY: The Master Container */
body {
    display: grid !important;
    grid-template-rows: 1fr auto !important; /* Row 1: Content (grows), Row 2: Footer (fitted) */
    min-height: 100vh !important;
    height: auto !important;       /* Allow it to grow infinitely */
    margin: 0 !important;
    padding: 0 !important;
    overflow-y: auto !important;   /* FORCE SCROLL: The fix for your stuck page */
    overflow-x: hidden !important; /* Prevent side-scroll */
}

/* 2. THE WRAPPER (Content + Sidebar) */
/* This sits in the first grid row and stretches */
.page-wrapper {
    width: 100%;
    max-width: 100%;
    display: flex !important;     /* Keep Sidebar next to Content */
    flex-direction: row !important; 
    justify-content: center;
    align-items: flex-start;      /* Align to top */
}

/* 3. MAIN CONTENT */
main.content {
    margin-top: 0 !important;
    padding-top: 60px !important; /* Space for Navbar */
    width: 100% !important;
    max-width: var(--layout-max) !important;
    flex: 1; /* Let it shrink/grow */
}

/* 4. THE FOOTER */
.custom-footer {
    grid-row: 2; /* Sit explicitly in the second row */
    width: 100% !important;
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    padding: 40px 0 !important;
    z-index: 10;
}

.footer-content {
    width: 100%;
    max-width: var(--layout-max);
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* 5. MOBILE & TABLET RESPONSIVENESS */
/* When screen is small, stack the sidebar underneath */
@media screen and (max-width: 1000px) {
    .page-wrapper {
        flex-direction: column !important;
        align-items: center !important;
    }
    
    /* Ensure main content doesn't get squashed */
    main.content {
        padding-left: 20px !important;
        padding-right: 20px !important;
    }
}

/* Typography */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
