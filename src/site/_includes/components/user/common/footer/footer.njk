<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. SETUP THE PAGE (The Stage) */
/* We force the body to leave room at the bottom so content doesn't get hidden */
body {
    position: relative;        /* The Anchor */
    min-height: 100vh;         /* Always full screen */
    padding-bottom: 140px !important; /* CRITICAL: Must be taller than the footer height! */
    box-sizing: border-box;
}

/* 2. THE FOOTER (The Actor) */
.custom-footer {
    position: absolute;        /* Ignore layout flow (Flex/Grid) */
    bottom: 0;                 /* Stick to the bottom of the body */
    left: 0;                   /* Start at the far left edge */
    width: 100%;               /* Span the full screen width */
    height: auto;              /* Let content define height */
    
    /* VISUALS */
    background-color: var(--background-secondary); /* Your desired Dark BG */
    border-top: 1px solid var(--background-modifier-border);
    padding: 30px 0;
    z-index: 9999;             /* Sit on top of everything */
    text-align: center;
}

/* 3. THE CONTENT (Center it nicely) */
.footer-content {
    max-width: var(--layout-max); /* Match your site's max width */
    margin: 0 auto;
    padding: 0 20px;
}

.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-ui);
    font-size: 0.9em;
    line-height: 1.5;
}

.footer-sub {
    font-size: 0.8em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}

.custom-footer a {
    color: var(--text-muted);
    text-decoration: underline;
    text-decoration-style: dotted;
}
.custom-footer a:hover {
    color: var(--interactive-accent);
}
</style>
