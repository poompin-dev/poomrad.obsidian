<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* =========================================
   THE NUCLEAR FIX
   ========================================= */

/* 1. BODY RESET: Prevent Horizontal Scroll & Vertical Stacking */
body {
    display: block !important;       /* Kills the "Flex Column" bug */
    overflow-x: hidden !important;   /* CRITICAL: Hides the scrollbar caused by the wide footer */
    width: 100% !important;
    position: relative !important;
}

/* 2. MAIN TEXT: Force Centering */
/* This grabs your article and shoves it to the middle */
main.content, .markdown-preview-view {
    display: block !important;
    margin-left: auto !important;
    margin-right: auto !important;
    float: none !important;
    
    /* Ensure it matches your preferred reading width */
    width: 100% !important;
    max-width: var(--layout-max) !important; 
    
    /* Spacing fixes */
    padding-top: 40px !important;
}

/* 3. FOOTER: Force Full Viewport Width (The Breakout) */
.custom-footer {
    /* This calculation forces the element to be exactly as wide as the screen */
    width: 100vw !important; 
    
    /* This pulls it out of any centered container */
    position: relative !important;
    left: 50% !important;
    right: 50% !important;
    margin-left: -50vw !important;
    margin-right: -50vw !important;
    
    /* Visuals */
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    margin-top: 5rem !important;
    padding: 40px 0 !important;
    z-index: 10;
}

/* 4. FOOTER CONTENT: Re-align to Match Text */
.footer-content {
    width: 100%;
    max-width: var(--layout-max); /* Same width as your main text */
    margin: 0 auto;               /* Center it inside the wide strip */
    padding: 0 20px;
    
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Typography */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9em;
}
.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}
</style>
