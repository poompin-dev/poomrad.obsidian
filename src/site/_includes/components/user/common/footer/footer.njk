<footer class="custom-footer">
  <div class="footer-content">
    <p>© <span id="year-display"></span> Dr.SFRad</p>
    <p class="footer-sub">
      Built with <a href="https://github.com/oleeskild/obsidian-digital-garden">Digital Garden</a>
    </p>
  </div>
</footer>

<script>
  document.getElementById("year-display").innerText = new Date().getFullYear();
</script>

<style>
/* 1. SETUP THE PAGE */
body {
    position: relative;
    min-height: 100vh;
    padding-bottom: 140px !important; 
    box-sizing: border-box;
}

/* 2. THE FOOTER CONTAINER */
.custom-footer {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: auto;
    background-color: var(--background-secondary);
    border-top: 1px solid var(--background-modifier-border);
    padding: 30px 0;
    z-index: 9999;
}

/* 3. THE INNER CONTENT BOX */
.footer-content {
    /* Use the same width logic as your main content so it aligns perfectly */
    width: 100%;
    max-width: var(--layout-max);
    margin: 0 auto; /* Centers the box on the screen */
    padding: 0 20px;
    
    /* FORCE CENTERING OF CHILDREN */
    display: flex;
    flex-direction: column;
    align-items: center; 
    justify-content: center;
    text-align: center;
}

/* 4. THE TEXT (The Fix) */
.footer-content p {
    margin: 0;
    color: var(--text-muted);
    font-family: var(--font-ui);
    font-size: 0.9em;
    line-height: 1.5;
    
    /* FORCE CENTER (Overrides theme defaults) */
    text-align: center !important; 
    width: 100%;
}

.footer-sub {
    font-size: 0.85em !important;
    opacity: 0.6;
    margin-top: 8px !important;
}

.custom-footer a {
    color: var(--text-muted);
    text-decoration: underline;
    text-decoration-style: dotted;
}
.custom-footer a:hover {
    color: var(--interactive-accent);
}
</style>
